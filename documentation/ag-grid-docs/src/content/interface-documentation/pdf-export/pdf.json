{
    "_config_": {},
    "PdfExportParams": {
        "documentTitle": {
            "description": "Document title stored in the PDF metadata. When set, a visible title is rendered above the exported table."
        },
        "documentTitleConfig": {
            "description": "Configuration for the visible document title (font, colours, spacing, border, alignment). Set `enabled=false` to suppress the visible title."
        },
        "pdfStyles": {
            "description": "Overrides PDF colours using theme-style keys such as `backgroundColor`, `headerBackgroundColor`, and `oddRowBackgroundColor`. Values accept CSS colours."
        },
        "prependContent": {
            "description": "Content to put at the top of the PDF export. A 2D array of PdfCell objects or a multi-line string that is split into rows."
        },
        "appendContent": {
            "description": "Content to put at the bottom of the PDF export. A 2D array of PdfCell objects or a multi-line string that is split into rows."
        },
        "fileName": {
            "default": "export.pdf"
        }
    },
    "PdfCell": {
        "data": {
            "isRequired": true
        }
    },
    "PdfCellData": {
        "value": {
            "isRequired": true
        }
    }
}
